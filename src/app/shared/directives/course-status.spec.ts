import { CourseStatus } from './course-status';

describe('CourseStatus', () => {
  it('should create an instance', () => {
    const directive = new CourseStatus();
    expect(directive).toBeTruthy();
  });
});
